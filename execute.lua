function execute(distance, ...) &&&&&&&&&&&&&&&&&&&&&&&&
    local diastance = distance or 1
    local check, reason = true, nil
    local distanceAbs = math.abs(distance)
    if distance>0 then
        for i, distance do
            for argument in pairs(arg) do
                if argument == 1 
            end
        end
    elseif diastance<0 then
        
    end
end