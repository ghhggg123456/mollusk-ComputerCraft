#### compare()
```
mollusk.compare([argument...])
```
Cursed function. Compare the block in front of turtle with the `argument`. `argument` can be number of slot or string id of block. 
If function is a number, it use standard turtle operation `compare()` (check if the block in front of the turtle is equal to the item in the currently selected slot).
If function is a  string, it check if the `argument` is equal id of block in that direction.
##### Parameters
1. argument...=1 : number | string The argument for comporasion
##### Returns
1. boolean If block is equal to any argument

#### compareLeft()
```
mollusk.compareLeft([argument])
```
Cursed function. Compare the block to the left of turtle with the `argument`. `argument` can be number of slot or string id of block. 
If argument is a number, it use standard turtle operation `compare()` (check if the block to the left of the turtle is equal to the item in the currently selected slot).
If argument is a string, it check if the `argument` is equal id of block in that direction.
##### Parameters
1. argument...=1 : number | string The argument for comparison
##### Returns
1. boolean If block is equal to any argument

#### compareRight()
```
mollusk.compareRight([argument])
```
Cursed function. Compare the block to the right of turtle with the `argument`. `argument` can be number of slot or string id of block. 
If argument is a number, it use standard turtle operation `compare()` (check if the block to the right of the turtle is equal to the item in the currently selected slot).
If argument is a string, it check if the `argument` is equal id of block in that direction.
##### Parameters
1. argument...=1 : number | string The argument for comparison
##### Returns
1. boolean If block is equal to any argument

#### compareBack()
```
mollusk.compareBack([argument])
```
Cursed function. Compare the block behind the turtle with the `argument`. `argument` can be number of slot or string id of block. 
If argument is a number, it use standard turtle operation `compare()` (check if the block behind the turtle is equal to the item in the currently selected slot).
If argument is a string, it check if the `argument` is equal id of block in that direction.
##### Parameters
1. argument...=1 : number | string The argument for comparison
##### Returns
1. boolean If block is equal to any argument

#### compareUp()
```
mollusk.compareUp([argument])
```
Cursed function. Compare the block above the turtle with the `argument`. `argument` can be number of slot or string id of block. 
If argument is a number, it use standard turtle operation `compare()` (check if the block above the turtle is equal to the item in the currently selected slot).
If argument is a string, it check if the `argument` is equal id of block in that direction.
##### Parameters
1. argument...=1 : number | string The argument for comparison
##### Returns
1. boolean If block is equal to any argument

#### compareDown()
```
mollusk.compareDown([argument])
```
Cursed function. Compare the block below the turtle with the `argument`. `argument` can be number of slot or string id of block. 
If argument is a number, it use standard turtle operation `compare()` (check if the block below the turtle is equal to the item in the currently selected slot).
If argument is a string, it check if the `argument` is equal id of block in that direction.
##### Parameters
1. argument...=1 : number | string The argument for comparison
##### Returns
1. boolean If block is equal to any argument